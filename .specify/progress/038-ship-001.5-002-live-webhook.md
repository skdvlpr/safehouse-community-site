# 038 — Ship 001.5 + 002/002.1; live webhook; analytics stay off

**Date**: 2026-09-18

Owner: micro-patch if safe, then commit+push to production including analytics **code**, keep measurement **off** on prod. Pre-check CRM events. Configure live Stripe if possible. Run tests before push.

## Micro-patch

One-time `createDonationIntent` sets `capture_method=automatic` so BalanceTransaction is usually present on `payment_intent.succeeded` ([create capture_method](https://docs.stripe.com/api/payment_intents/create#create_payment_intent-capture_method)). Does not replace 001.5: missing BT still 200 pending; `charge.updated` still ingests. Recurring invoice PIs stay Billing defaults. Cite: [asynchronous capture](https://docs.stripe.com/payments/payment-intents/asynchronous-capture), [Payment Element](https://docs.stripe.com/payments/payment-element).

## Live Stripe (agent applied)

Account `acct_1TfltZ…` (Safe House).

| Mode | Endpoint | Status | Change |
| :--- | :--- | :--- | :--- |
| **Live** | `https://safehouse.community/api/webhooks/stripe` | **enabled** | Added `charge.updated` to existing ingest+status list ([update webhook](https://docs.stripe.com/api/webhook_endpoints/update)) |
| Test | same URL | **disabled** (left off) | Added `charge.updated` for when someone later uses test keys; must not enable while live keys are on prod |

URL and signing secret unchanged. No Caddy apply. No legal-page sync (`site:sync-legal-pages`).

Site already maps ingest (`payment_intent.succeeded`, `invoice.paid`, `charge.updated`) and status (refund/dispute/cancel/fail/payout/subscription.deleted). Live allowlist now matches.

## Analytics stay off on prod

`MEASUREMENT_ENABLED` default false ([Laravel config](https://laravel.com/docs/13.x/configuration)). CMS toggle exists after deploy; do not turn it on. `post-deploy.sh` does not seed legal pages or measurement settings. Do not apply Caddy GTM hosts until measurement is on.

Official this note: async capture, capture_method, webhook update, event types, Payment Element, Laravel testing, Pint.
