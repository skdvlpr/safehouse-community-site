# 010 — Specified 007 visual refresh and 008 instance identity (end of queue)

**Date:** 2026-09-14  
**Command:** `/speckit-specify` (owner added two rows at the **end** of the SITE queue)

## Queue (do not skip)

1. `001.2` audit repairs (next to specify/implement when the owner says so)
2. `002` analytics → `003` SEO → `004` ads → `005` banners → `006` GDPR
3. `007-public-visual-refresh` — public typeface + liveliness; CMS look unchanged
4. `008-local-prod-distinction` — technical preview vs live identity; **no** public banner

## Owner answers encoded

- Packaging: two rows, not a mega-spec.
- Visual: keep Aurora; replace programmer-editor public typeface with a similar sans; more motion and depth; details at plan time.
- Environment: **config/status**, not on-page chrome.

## Official docs (environment)

- [Laravel configuration](https://laravel.com/docs/13.x/configuration) — `APP_ENV` / `config('app.env')`
- DDEV injects project identity into the web container (`IS_DDEV_PROJECT` and related vars); `web_environment` in this repo is currently empty.

## Active spec pointer

`feature.json` remains `specs/001.1-prod-parity-replan` until the owner opens `001.2` or later. Do **not** `/speckit-plan` 007/008 until the queue reaches them.
