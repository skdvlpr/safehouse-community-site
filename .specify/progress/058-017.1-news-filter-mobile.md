# 058 — UAT repair `017.1-news-filter-mobile`

**Date**: 2026-09-25
**Agent**: Cursor Grok 4.6
**Active spec**: `specs/017.1-news-filter-mobile/`

## Current state

Owner production screenshots: Notizie/Articoli phone filter bar overflow. **Data** clipped to **A**, **Applica** cut off. Date group was `flex-nowrap` + `shrink-0` in one row with the label.

CSS now stacks groups on small screens; date fields `min-w-0` in a two-column grid; **Applica** full width under them. Desktop row unchanged.

Waiting on owner UAT. No commit unless asked.

## Verification

NewsHeadingTest + pint + frontend rebuild this turn.
