# 033 — plan 001.5 donation settled CRM

**Date**: 2026-09-18

**Feature**: `specs/001.5-donation-settled-crm`

**Setup**: `setup-plan.sh --json` → FEATURE_DIR `specs/001.5-donation-settled-crm`, BRANCH name `001.5-donation-settled-crm` (spec dir; no git commit).

## Design

- Status webhooks stay first (`applyFromStripeEvent`).
- Ingest on `payment_intent.succeeded` / `invoice.paid` **and** `charge.updated` when BT usable + site donation metadata.
- Settlement not ready → HTTP **200** pending, not 502.
- Thank-you: one-shot ingest, no 20×500ms block.
- Live `charge.updated` allowlist = owner-ops.

Official this plan: [async capture](https://docs.stripe.com/payments/payment-intents/asynchronous-capture), [webhooks](https://docs.stripe.com/webhooks), [signatures](https://docs.stripe.com/webhooks/signatures), [event types](https://docs.stripe.com/api/events/types), [listen](https://docs.stripe.com/cli/listen), [Payment Element](https://docs.stripe.com/payments/payment-element), [Laravel testing](https://laravel.com/docs/13.x/testing), [logging](https://laravel.com/docs/13.x/logging), [Pint](https://laravel.com/docs/13.x/pint).

## Artifacts

- `plan.md`, `research.md`, `data-model.md`, `quickstart.md`
- `contracts/http.md`, `contracts/owner-ops.md`

## Next

`/speckit-tasks` then owner model table wait, then `/speckit-implement`.
