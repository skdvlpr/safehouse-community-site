# 036 — UAT 001.5 TEST 6 502 then resend

**Date**: 2026-09-18

Listen: `payment_intent.succeeded` **200 pending** (001.5 OK). Thank-you skipped settlement. Later `charge.updated` **502** because Espo had **two Contact** rows for the same donor channels (`EspoCrmPartyResolver` throws; not missing fee). Stripe PI has BT fee/net. TEST 5 still not ingested (no resend).

Owner merged duplicate Contact. Agent replayed `evt_3UH4cL1XvMWH96Ks0fnYpivm` as signed POST to DDEV (Dashboard `we_1TojcB…` is disabled + production URL; CLI `events resend` would not hit listen). Response **200 OK**. Assigned-user CMS id still missing in local Espo (fallback warning, out of 001.5).

Live Dashboard: add event type `charge.updated` to the existing endpoint; do not change the URL. Not applied this turn.
