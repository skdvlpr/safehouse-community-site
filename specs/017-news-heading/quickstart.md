# Quickstart: News heading filters

Open the page in Italian on a wide window and on a phone. Heading matches the spec. No red label above the title. Diventa socio is unchanged.

```bash
ddev exec php artisan test --filter=PageHeading
```
