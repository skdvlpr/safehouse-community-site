# Contract: Phone listing toolbar

Cite: [min-width](https://tailwindcss.com/docs/min-width), [grid-template-columns](https://tailwindcss.com/docs/grid-template-columns), [Blade](https://laravel.com/docs/13.x/blade).

`GET /it/news` and `GET /it/articles` include `news-toolbar`, `news-toolbar__group--dates`, `news-date-filters`, and `news-date-filters__submit`. Phone layout is CSS (`md:` row). `landing.blade.php` is out of contract.
