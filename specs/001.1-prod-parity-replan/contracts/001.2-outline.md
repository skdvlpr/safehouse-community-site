# 001.2 outline — audit repairs (not this implement)

Specify later as `specs/001.2-audit-repairs/` after 001.1 owner UAT. **Do not start until the owner says specify/implement 001.2.**

Official: [Stripe webhook signatures](https://docs.stripe.com/webhooks/signatures) · [Laravel CSRF](https://laravel.com/docs/13.x/csrf) · [Caddy header](https://caddyserver.com/docs/caddyfile/directives/header)

## In 001.2

| ID | Work | Notes |
| :--- | :--- | :--- |
| F-002 | Invalid Stripe webhook signatures MUST return 4xx, not 500 | Test **locally** first. CRM statuses already work. **Tell the owner in chat when to raise `stripe listen` / Stripe sandbox.** Do not change production webhooks blindly. |
| F-003 | Donor-facing strings translation-ready | it + en only |
| F-004 | Volunteer Turnstile (and re-check production contact: widget absent 2026-09-13) | |
| F-005 | Align framing headers PHP vs Caddy | Live snippet apply is **owner-ops** (root) |
| F-007 | HomeController constructor injection | |
| F-008 | Empty `catch (Throwable)` in PrimaNota payment status | Prod log note already in inspect contract |
| F-009 | Checkout JSON must not leak raw exception messages | |
| F-010 | CSRF except vs `preventRequestForgery` ([Laravel CSRF](https://laravel.com/docs/13.x/csrf)) | |
| F-011 | Cookie consent IP hash (HMAC if required) | |
| F-012 | `trustProxies` review | |
| F-013 | CMS last-error dump hygiene | |
| F-014 | Layout title suffix translatable | |

## Not in 001.2

- Checkout Sessions / Payment Element migration (`later-checkout` + amend `S-STRIPE` only then)
- Production Caddy HSTS/CSP/CMS allowlist (`owner-ops`)
- Analytics / SEO / ads landings / banners / GDPR (`002`–`006`)
- Rewriting git history for old `sk_test_` commits

## Local Stripe listen (when 001.2 tests start)

Local CMS already has test integration settings. Before webhook tests, the implementer MUST write in owner chat: raise Stripe CLI listen / switch Dashboard to sandbox. Do not assume it is running.
